project:
  name: "RoRAG-Trust"
  device: "cuda" # sau "cpu"

retrieval:
  top_k: 5
  chunk_size: 512
  overlap: 50

verification:
  nli_model: "cross-encoder/nli-deberta-v3-base" # Acesta este un model standard, foarte capabil
  abstention_threshold: 0.75
  
generation:
  model_name: "gpt-4o" # sau un model local precum Mistral/Llama3
  temperature: 0.0