project:
  name: "RoRAG-Trust"
  device: "cuda" 

retrieval:
  top_k: 5
  chunk_size: 512
  overlap: 50

verification:
  nli_model: "cross-encoder/nli-deberta-v3-base" 
  abstention_threshold: 0.75
  
generation:
  model_name: "gpt-4o" 
  temperature: 0.0